# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# remote-app/dist is hand-authored source (no build step for the phone PWA),
# not a build artifact — keep it tracked despite the blanket `dist` rule above.
!remote-app/dist
!remote-app/dist/**

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
